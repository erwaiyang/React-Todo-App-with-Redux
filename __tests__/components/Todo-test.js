jest.unmock('../../src/components/Todo');

import Todo from '../../src/components/Todo';

import React from 'react';
import ReactDOM from 'react-dom';
import TestUtils from 'react-addons-test-utils';

describe('components', () => {
  describe('Todo', () => {
    it('generates an input when its text is clicked', () => {

    });
    it('triggers STAR_TODO when star icon is clicked', () => {

    });
    it('triggers COMPLETE_TODO when check icon is clicked', () => {

    });
    it('triggers DEL_TODO when trash icon is clicked', () => {

    });
  });
});
